<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>

<body>
  <div class="detail-wrap">
    <div class="detail-continer">
      {{#showDelete}}
      <a class="detail-delete" href="/recipe/delete/{{recipe.id}}">X</a>
      {{/showDelete}}
      <div class="detail-title">{{recipe.getTitle}}</div>
      <hr>
      {{#recipe.recipetags.size}}
      <div class="detail-tag-container" class="detail-tag-container">
        {{#recipe.recipetags}}
        <div class="detail-tag-name">{{tags.name}}</div>
        {{/recipe.recipetags}}
      </div>
      <hr>
      {{/recipe.recipetags.size}}
      <div class="detail-content-body">{{recipe.getBody}}</div>
    </div>
  </div>
</body>
<style>
  .detail-wrap {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .detail-continer {
    position: relative;
    width: 50vw;
    margin-top: 100px;
    padding: 20px;
    background: #ffffff;
    -webkit-border-radius: 4px 4px 4px 4px;
    -moz-box-shadow: 0 0 4px #888;
    -webkit-box-shadow: 0 0 4px #888;
    box-shadow: 0 0 4px #888;
  }

  .detail-delete {
    position: absolute;
    right: 20px;
    top: 20px;
    scale: 1;
    z-index: 2;
    color: inherit;
    text-decoration: none;
    cursor: pointer;
  }

  .detail-tag-container {
    display: flex;
  }

  .detail-title {
    font-size: 24px;
  }

  .detail-tag-name {
    margin-right: 4px;
  }

  .detail-content-body {
    margin-bottom: 20px;
  }

  hr {
    margin: 20px 0;
    border: 0;
    border-top: 1px solid #eee;
    border-bottom: 1px solid #fff;
    display: block;
    unicode-bidi: isolate;
    margin-block-start: 0.5em;
    margin-block-end: 0.5em;
    margin-inline-start: auto;
    margin-inline-end: auto;
    overflow: hidden;
    border-style: inset;
    border-width: 1px;
  }
</style>

</html>